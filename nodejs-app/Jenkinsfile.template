pipeline {
   agent { label 'nodejs-app'}
   options { 
   buildDiscarder(logRotator(numToKeepStr: '2'))
  }
   stages {
      stage('Say Hello') {
         steps {
          container('nodejs') {
            echo 'Hello World!'   
            sh 'node -version'
          }
          sh 'java -version'
         }
      }
   }
}
